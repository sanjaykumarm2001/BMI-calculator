- hosts: node
  become: true
  tasks:
    - name: Create new deployment
      command: minikube kubectl -- apply  /home/ubuntu/deployment.yaml

    - name: Create new service
      command: minikube kubectl -- apply  /home/ubuntu/service.yaml
